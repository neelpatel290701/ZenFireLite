<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZenFireLite" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
<!--         <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowEnterTransition">@android:transition/slide_right</item>
        <item name="android:windowExitTransition">@android:transition/slide_left</item>
    </style>

    <style name="Theme.ZenFireLite" parent="Base.Theme.ZenFireLite" />


    <style name="MyDatePickerDialogStyle" parent="Theme.AppCompat.Light.Dialog">
        <!-- Customize the calendar color -->
        <item name="colorAccent">@color/black</item>
        <!-- Customize the calendar text size -->
        <item name="android:textSize">16sp</item>
    </style>



    <style name="ThemeOverlay.AppCompat.navTheme">
        <!-- Color of text and icon when SELECTED -->
        <item name="colorPrimary">@color/white</item>
        <!-- Background color when SELECTED -->
        <item name="colorControlHighlight">@color/Zen_Red</item>
        <!-- Color of text when NON-SELECTED -->
        <item name="android:textColorSecondary">@color/black</item>
    </style>


    <style name="RoundedTabLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@drawable/tabs_selector</item>
        <item name="tabIndicator">@null</item>
        <item name="android:background">@drawable/tab_layout_bg</item>
        <item name="tabSelectedTextColor">@color/black</item>
        <item name="tabTextColor">@color/white</item>
    </style>


    <!-- Custom TextAppearance for Toolbar Title -->
    <style name="CustomToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">20sp</item> <!-- Set the desired text size -->
    </style>


</resources>